# Required environment variable for command-line letter generation.
# This project does not automatically load .env files.
# For Streamlit, use .streamlit/secrets.toml or the hosted app's Secrets.
# Never commit a real key.
OPENAI_API_KEY=
