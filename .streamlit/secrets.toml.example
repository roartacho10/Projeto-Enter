# Copy to .streamlit/secrets.toml (gitignored) to stop the app asking for a key
# on every visit. On Streamlit Community Cloud, paste the same two lines into
# Settings > Secrets instead - nothing about the key belongs in the repository.
OPENAI_API_KEY = ""
MODEL_LETTER = "gpt-4.1"
